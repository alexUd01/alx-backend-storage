// A script tjat adds a new attribute to a document in the collection `school`
// The script should update only document with name="Holberton school" (all of them)
// The update should add the attrivute `address` with the value "971 Mission Street"
// THe datacase name will be passed as an option of `mongo` command
db.school.updateMany({"name":"Holberton school"}, {$set: {"address":"972 Mission street"}})
